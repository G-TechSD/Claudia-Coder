# =============================================================================
# Claudia Coder - Windows Installation Environment Variables
# =============================================================================
# Copy this file to .env and customize for your setup
# Usage: Copy to .env in the same directory as docker-compose.yml
# =============================================================================

# =============================================================================
# Required Settings
# =============================================================================

# Auth Secret - CHANGE THIS IN PRODUCTION!
# Generate with PowerShell: [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
# Or with OpenSSL: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-in-production-use-openssl-rand-base64-32

# =============================================================================
# API Keys (Optional)
# =============================================================================

# Anthropic Claude API - For AI features
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Linear API - For project management integration
# Get your key from: https://linear.app/settings/api
LINEAR_API_KEY=

# =============================================================================
# n8n Configuration
# =============================================================================

# n8n API Key - Set after first n8n login
N8N_API_KEY=

# n8n Basic Auth (change these!)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=claudia

# =============================================================================
# Email Configuration (Optional - for invite system)
# =============================================================================

# Option 1: Resend (recommended)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=

# Option 2: SMTP
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Sender address
EMAIL_FROM=Claudia <noreply@claudiacoder.com>

# =============================================================================
# Timezone
# =============================================================================

# Set your timezone (used by n8n and other services)
# Examples: America/New_York, Europe/London, Asia/Tokyo
TIMEZONE=UTC

# =============================================================================
# Advanced Configuration
# =============================================================================

# GitLab root password (set on first initialization)
# If not set, check the container logs for auto-generated password
# docker logs claudia-gitlab 2>&1 | grep "Password:"
GITLAB_ROOT_PASSWORD=

# LM Studio ports (if running on non-default ports)
# LMSTUDIO_PORT_1=1234
# LMSTUDIO_PORT_2=1235

# Ollama port (if running locally)
# OLLAMA_PORT=11434
