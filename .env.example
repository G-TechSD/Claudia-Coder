# ===========================================
# Claudia Coder Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
#
# NOTE: For the all-in-one Docker container, these values are
# pre-configured. You only need this file for standalone installs.

# ===========================================
# Bundled Services (All-in-One Container)
# ===========================================
# These URLs are pre-configured for the all-in-one container.
# Integration settings will show "Connected" immediately after install.

# Whisper STT Server (local)
NEXT_PUBLIC_WHISPER_URL=http://localhost:8000

# n8n Workflow Automation
NEXT_PUBLIC_N8N_URL=http://localhost:5678
NEXT_PUBLIC_N8N_API_KEY=your-n8n-api-key
N8N_WEBHOOK_URL=http://localhost:5678/webhook/claudia-execute
N8N_API_URL=http://localhost:5678/api/v1
CLAUDIA_CALLBACK_URL=http://localhost:3000/api/n8n-callback

# Gitea Git Server (bundled in all-in-one)
NEXT_PUBLIC_GITEA_URL=http://localhost:3000

# OpenWebUI Chat Interface (bundled in all-in-one)
NEXT_PUBLIC_OPENWEBUI_URL=http://localhost:8080

# Ollama LLM Server (bundled in all-in-one)
NEXT_PUBLIC_OLLAMA_URL=http://localhost:11434

# ===========================================
# External Services (Optional)
# ===========================================

# LM Studio Servers
NEXT_PUBLIC_LMSTUDIO_BEAST=http://localhost:1234
NEXT_PUBLIC_LMSTUDIO_BEDROOM=http://localhost:1234

# Linear (optional)
NEXT_PUBLIC_LINEAR_API_KEY=

# GitLab (optional - for external GitLab, not bundled Gitea)
NEXT_PUBLIC_GITLAB_URL=https://gitlab.example.com

# Anthropic Claude API (for interview questions)
ANTHROPIC_API_KEY=

# ===========================================
# Better Auth Configuration
# ===========================================

# Application URL
NEXT_PUBLIC_APP_URL=https://localhost:3000

# Auth secret - CHANGE THIS IN PRODUCTION
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-change-in-production

# ===========================================
# Email Configuration (for invite system)
# ===========================================
# Choose ONE of the following email providers:

# Option 1: Resend (recommended - https://resend.com)
# Get your API key from https://resend.com/api-keys
RESEND_API_KEY=

# Option 2: SMTP (e.g., Gmail, SendGrid, Mailgun)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# Email sender address
EMAIL_FROM=Claudia <noreply@claudiacoder.com>
